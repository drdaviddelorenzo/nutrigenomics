# Only required for figure generation; the analysis itself uses the standard
# library alone. Upper bounds keep a future major release from silently
# changing behaviour. reportlab was removed in 0.3.5: it was never imported.
pandas>=2.2,<3.0
numpy>=1.26,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
